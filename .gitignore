# Xcode
build/
dist/
DerivedData/
*.xcuserstate
xcuserdata/
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# macOS
.DS_Store

# Swift Package Manager
.build/
.swiftpm/
Packages/

# Whisper Relay
server/node_modules/
server/.env

# Personal Claude Code permissions (names the deploy host; not team config)
.claude/settings.local.json

# Codex's local environment config — self-declared autogenerated, same category as the
# Claude entry above. Both are editor state, not something the repo should carry.
.codex/
